[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi[all]>=0.115.12",
    "httpx>=0.28.1",
    "invoke>=2.2.0",
    "pyinstaller>=6.14.0",
]
